# kangmin-core

抗敏先锋的纯领域与应用能力包，不包含数据库、HTTP、CLI、模型或对象存储实现。

- `src/kernel/`：协议、错误、验证、凭据和加密端口
- `src/patient/`：患者账号、记录与患者应用编排
- `src/intelligence/`：智能问答与确定性临床规则
- `src/content/`：内容浏览与环境信息
- `src/operations/`：后台内容、知识、用户管理与管理应用编排
- `tests/`：包级测试说明；现有行为测试迁移期间仍由根测试区统一运行

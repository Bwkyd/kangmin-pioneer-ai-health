# CLAUDE.md - 抗敏先锋 AI 鼻健康管理系统开发必读（kangmin）

> 开工先读本文件、`.42cog/` 四份、`state/board.md` 和 `state/memory/MEMORY.md`。
> 系统事实只写进 `.42cog/`，产出标准只写进 `specs/`，当前状态只写进 board。
> 本项目以 Codex 为主要 Agent，`AGENTS.md` 是主入口；`CLAUDE.md` 仅作 Claude Code 兼容副本。
> `hi.md` 是作者手稿：只读，不修改、不重排、不格式化、不添加提示语。

## 一、只从唯一正本读取方向与事实

- 从 `.42cog/intent.md` 读取收敛方向、真难题、不做什么和验证闭环；不要在其他文件复制。
- 从 `.42cog/real.md` 读取日程、红线、资源和使用者现实。
- 从 `.42cog/cog.md` 读取实体、参与者、输入输出和变化传播。
- 从 `.42cog/meta.md` 读取系统身份、位置、交接和依赖。
- 从 `state/board.md` 读取每轮变化、验证、遗留、待办和 commit；按倒序追加新轮次。
- 从 `meta/kangmin_directory-protocol.md` 读取完整目录与命名协议。

把 `vault/truth/` 作为开发与验收唯一资料真相源。范围、所属端、价格和工期服从
`vault/truth/抗敏先锋AI小程序报价表.md`；题面、证型与期别、调理方法和视频分别服从
对应专项 truth。不要让 `docs/`、`state/`、代码、测试或 AI 推断反向覆盖 truth；同一职责
仍冲突或缺失时停止推断，登记决策点，等作者确认并写回 truth。

## 二、按开工协议分流

按“确定性分流 → 冒烟确认 → 必要时有界实验”的顺序开始：

1. 先判断任务是只读回答、诊断，还是明确要求实现。
2. 检查当前分支、工作区、worktree、近期提交、目标文件和权限，不覆盖用户已有改动。
3. 直接执行范围内可确定、可逆、低风险的工作；把会改变方向、范围、口味、医学规则或
   对外状态的决策点前置给作者。
4. 结构或方案存在真实分叉时，先在 `docs/experiments/` 做有界实验，记录假设、成本、
   证据与限制，再决定方案。
5. 同类任务合并处理；单项卡住时继续推进其他安全工作。同类错误第三次出现时，把规则
   固化进本文件、`state/memory/` 或 `scripts/`。

破坏性操作、批量删除、强制覆盖、强推、发布、合并、部署和外部写操作必须先取得明确
授权。`ALLOW_MAIN_PUSH=1` 只允许作者显式授权后人工使用，AI 不得自行设置。

## 三、按六组目录归位

| 组 | 位置 | 怎么做 |
| --- | --- | --- |
| 开工手册与规约 | `README.md`、本文件、`.42cog/`、`specs/` | 让 README 面向人、本文件约束 AI、`.42cog` 只陈述事实、`specs` 只定标准 |
| 真相源与参考 | `vault/`、`notes/`、`resources/` | 最小读取 truth；raw 与 notes 只读；resources 读懂后自己写，不复制 |
| 脚本与扩展 | `skills/`、`.agents/skills/`、`.codex/`、`.claude/workflows/`、`scripts/`、两份插件清单 | 技能正文保留唯一正本，按工具发现规则接入；机械判断写脚本 |
| 作品 | `src/` | 在当前主实现完成可验证交付 |
| 状态与文档 | `state/`、`docs/`、提交链 | 按寿命和职责落档，不把对话当持久状态 |
| 过程材料 | `_build/`、`_work/`、`_archive/` | 只放日期打头的目录，不放裸文件，不成为运行依赖 |

把 `legacy/` 仅作为迁移前需求、行为和验收参考，不向 `src/` 导入其业务实现。把 `_work/`
作为本项目对模板 `_tmp/` 的本地替代。不要修改或提交 `notes/`、`spce/`、vault 私密资料
和忽略区内容，除非作者明确指定目标与用途。

## 四、先建方法，再扩作品

- 把已经验证、输入输出稳定且值得复用的流程放入 `skills/<name>/SKILL.md`，并从
  `.agents/skills/<name>` 以相对符号链接接入 Codex，禁止复制两份正文。
- 把 Codex 项目设置放入 `.codex/config.toml`；让根目录 `AGENTS.md` 保持主入口，
  `CLAUDE.md` 只作为同层缺少 `AGENTS.md` 时的兼容回退。
- 按 `km-init`、`km-research`、`km-mini`、`km-evolve` 四步位组织项目元能力；未建立的技能
  明确标“待建”，不要伪装可用。
- 把命名、结构、格式、链接、清单等可机械判断的检查放入 `scripts/`。
- 修改 `plugin.json` 或 `42plugin.json` 后运行 `python3 scripts/check-manifests.py`。
- 不预先包装没有复用证据的流程；第一次踩坑先记录，第二次重复再考虑脚本或技能。

## 五、按三层降级处理工具缺失

1. 先运行 `bash scripts/check-tools.sh`，只确认项目选定工具是否存在。
2. 同生态入口不可用时，再采用项目已允许的替代入口或不依赖该工具的做法，并记录限制。
3. 只有官方源不可用时才查看 `bash scripts/check-tools.sh --mirrors`，优先可信高校镜像。

只从官方或可信来源安装，安装前核对包名和维护状态。三层都无法继续时停止试错，向作者
报告缺失工具、已验证路径与具体阻塞。

## 六、控制并行与评审

- 使用 Git worktree 并行，活动 worktree 总数控制在 4 个以内；创建前先 fetch，并确认磁盘。
- 不在 worktree 中继续套 worktree；忽略文件不会自动出现，不让任务依赖未准备的忽略件。
- 同一共享接口、数据库迁移、权限、临床规则或核心文件只安排一个集成 owner。
- 主要使用 Codex 可发现的 `$km-review` 做多视角评审；使用 Claude Code 时才调用
  `.claude/workflows/km-review.js`。两者只检查事实、逻辑、安全与合规，不裁决作者风格。
- 按 P0–P3 汇总；P0、P1、P2 清零后停止继续评审，P3 由作者决定。
- 把发送给外部模型的提示词先写入过程文件再通过标准输入或文件参数传递；发送前移除密钥、
  隐私和未获授权公开的内容。

## 七、按三层验证收工

| 层 | 谁判断 | 使用方式 |
| --- | --- | --- |
| 规则验证 | 脚本 | 运行清单、结构、类型、测试和链接检查 |
| 判据验证 | AI 按书面标准 | 使用技能判据、`specs/` 和 truth 检查质量与边界 |
| 实效验证 | 作者、客户与真实环境 | 由作者定标、客户验收；AI 不冒充拍板 |

按改动范围选择最低门禁：

- 文档与目录：`python3 scripts/check-manifests.py`、`python3 scripts/structure-lint.py .`、
  `git diff --check`。
- `src/`：先跑相关窄测，再运行 `cd src && npm run check`。
- `legacy/`：先跑相关窄测，再运行 `cd legacy && npm run check`。
- UI：增加真实浏览器路径、控制台与网络检查以及必要截图。
- 数据库、对象存储、权限、临床、核心接口或部署：增加真实适配器和失败路径验证及独立审核。

如实报告未配置 PostgreSQL、S3 或其他外部资源导致的跳过项。不要把测试通过扩大解释为
客户验收、临床批准、生产就绪、合并或部署授权。

## 八、守住患者智能体与医学边界

- 把本项目智能体定位为患者科普问答型：通用科普、问卷引导、规则判定后的解释与追问。
- 让确定性规则处理问卷顺序、证型、分期和方案匹配；只让模型做受控候选提取、知识问答
  和通俗转译。
- 不让模型自行诊断、改变判断标准、编造名单外穴位、疗法、禁忌、次数、时长或力度。
- 对高风险、规则冲突、关键临床信息不足和未知状态 fail-closed；`unknown` 不等于 `no`。
- 普通科普无检索命中时允许模型使用可靠的一般医学知识正常回答，不把模型回答提升为证型、
  分期、方案或临床规则，也不向患者暴露检索状态。
- 不把知识库检索资料自动提升为临床规则；只把作者确认并按职责写入 truth 的内容作为裁决。
- 不泄露患者健康数据、微信凭据、模型密钥或客户资料；试用优先使用模拟或无身份数据。

## 九、维护状态、文档与记忆

- 每轮有效项目工作都在 `state/board.md` 顶部新增轮次，记录实际变化、验证、遗留和 commit。
- 只在作者认定的重要里程碑更新 `state/changelog.md`，每条回答“变了什么、为什么、意味着什么”。
- 把需追溯的计划、调研、评审和实验放进对应 `docs/` 分类；把客户拍板事项放进
  `docs/product/YYYY-MM-DD-<topic>-decision.md`。
- 把经证据复核、可跨任务复用的新模式去重后写进 `state/memory/`，并同步 MEMORY 索引；
  临时状态、一次性数字和待办不要写进长期记忆。
- 遵守三种命名法：档案用三位流水号，过程目录用创建日期，记忆用最后更新日期；稳定规约
  不编号、不带日期，版本写在文件内部。
- 单次回复以 400 token 为目标；长内容按寿命分流：一次性进 `_work/`，状态进 board，
  经验进 memory，需追溯内容进 docs。

## 十、按项目 Git 规则交付

- 不自动提交、推送、创建 PR、合并、部署或清理，除非用户明确要求对应动作。
- 提交前检查 `git diff`、`git diff --check`、`git status --short`、敏感信息和生成文件。
- 一个 commit 只处理一个可独立说明、验证和回退的主题；标题使用中文祈使句，正文说明
  原因和关键取舍。
- 每个 commit 使用三行 ASCII trailer：

```text
Change-By: human | ai | ai+human
Agent: 实际执行改动的 Agent；纯人工填 none
Model: 实际使用的模型；纯人工填 none
```

不要伪造未经本人确认的 `Reviewed-By`、`Co-authored-by`、`Suggested-By` 等信息。修复评审
意见时保留独立 commit；PR 是否 squash 由作者决定。

任务 worktree 只有在提交已推送、PR 已合并、目标分支已核验包含最终成果、工作区干净且
没有需保留的独有提交时才满足清理条件。Squash merge 同时核对 PR 状态与最终代码；未经
作者确认，不删除 worktree 或本地、远端分支。

## 十一、遵守铁律

1. 允许第一版方向粗糙，但不允许收敛方向空白。
2. 把 `hi.md` 视为作者原件，永远只读。
3. 把 `vault/raw/`、`notes/` 和 `resources/` 视为只读材料；resources 读懂后自己写。
4. 以人手为界保留原件；未经人手的可重建试错才可丢弃。
5. 让旧实践服从新理念；旧产物只能证明事实，不能反向修改新标准。
6. 同一内容只保留一个唯一正本，其余文件用路径指向；历史交给提交链，不建 `v2`、`final` 副本。
7. 单文件超过 10 MB 先核实存储方案，超过 50 MB 不进普通 Git；仓内只留路径、哈希和说明。
8. 新顶层目录视为协议事件：先确认作品如何验证、真相源是谁，再更新协议与 changelog。
9. 整理作者意图时不扩写稀释强信号，不把 AI 补全冒充作者决定。
10. 密钥、个人隐私和客户私密资料不进 Git；删除、公开、批量改名等不可逆动作先确认。

## 十二、保持双入口一致

使用中文沟通、注释、文档与提交，文件名默认英文。优先索引 `AGENTS.md`；只有在说明
Claude Code 专属行为时才单独索引 `CLAUDE.md`。修改本文件后同步 `CLAUDE.md`，修改兼容
副本时反向同步本文件；除首行标题外必须完全一致，由 `scripts/structure-lint.py` 机械校验。

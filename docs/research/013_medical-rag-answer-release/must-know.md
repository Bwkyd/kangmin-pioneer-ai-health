# 决策点 · 医疗 RAG 命中失败、模型越界与答案发布门的近期业界实践

> 2026-08-22。**这一份是给 AI 用的裁刀，不是给你答的题。**
> 先写下不可妥协的几条，让候选逐格回答；**允许大量功能根本不读**。
> ⚠️ 三条草稿由 AI 先填，**你只改这三条**。改完再往下走。

## 我要作的决定

医疗 RAG 命中失败、模型越界与答案发布门的近期业界实践

## 一、这件事上，我不可妥协的是哪几条

逐条编号。每条要能被「原生支持 / 部分 / 没有」回答，答不了的说明写得太虚。

| # | 不可妥协的 | 为什么它是命门 | 怎么算满足 |
|---|---|---|---|
| 1 | 把“没检索到”“检索质量差”“生成越界”“急症漏路由”分开 | 四种故障的修法相反，混成一个固定兜底只会掩盖根因 | 每次试次能分别记录路由、检索、逐声明依据和最终发布结果 |
| 2 | 高风险医学事实仍 fail-closed，但不能因一条坏声明丢掉全部安全核心 | 放宽整段门会伤患者，维持整段门又会让有据答案不可用 | 逐声明保留/删除，依赖被破坏时只做一次受限重写，二次失败才降级 |
| 3 | 发布条件不能只信同一个生成模型自检 | 当前实验已证明模型会漏掉自己的越界；高风险场景不能靠概率性自信裁决 | 急症和批准事实用确定性规则；依据性用独立校验信号；真实多轮样本持续定标 |

## 二、知道了它们，我要作的是哪一个决定

选择下一版发布链：继续整段拦截、购买通用 Guardrail，还是实现风险路由后的逐声明发布与局部修复。

## 三、哪些看起来相关，但知道了也不改变这个决定

**排除项。一条不看的，抵十条要看的。**

- 不比较通用模型综合榜单；当前瓶颈不是模型是否“聪明”，而是如何约束患者可见结果。
- 不扩大到诊断型医疗产品、监管申报或训练专用医疗模型。
- 不研究完整知识图谱；当前手册证据与批准方案已经足够复现故障。

## 四、我的预判

**不必假装客观。** 写下你现在的猜测，让 AI 回源码去推翻它。

| 我怀疑 | 请核实 |
|---|---|
| 主流做法已从“整段 RAG 命中即可信”转向检索与生成分开评估 | 查 RAGChecker、Google Check Grounding 与 CRAG 的一手说明 |
| 商用 Guardrail 能直接替换本项目发布门 | 查 AWS、Azure、NVIDIA 的适用范围、粒度、语言与流式限制 |
| 医疗场景仍需把个体化建议交给确定规则或专业人员 | 查 HealthBench 与 g-AMIE 的评测、监督边界 |

> 通行建议是隐藏立场以免诱导模型。这里反过来——**亮出立场，但把任务改造成可证伪的核验。**

---

**放行**：三条命门都填了、都能被「有 / 部分 / 没有」回答，就可以开始取材。

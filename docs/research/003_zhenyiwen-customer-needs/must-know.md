# 决策点 · 客户的诊一诊需求是什么，以及下一件最该做的工作是什么

> 2026-08-14。**这一份是给 AI 用的裁刀，不是给你答的题。**
> 先写下不可妥协的几条，让候选逐格回答；**允许大量功能根本不读**。
> ⚠️ 三条草稿由 AI 先填，**你只改这三条**。改完再往下走。

## 我要作的决定

客户的诊一诊需求是什么，以及下一件最该做的工作是什么

## 一、这件事上，我不可妥协的是哪几条

逐条编号。每条要能被「原生支持 / 部分 / 没有」回答，答不了的说明写得太虚。

| # | 不可妥协的 | 为什么它是命门 | 怎么算满足 |
|---|---|---|---|
| 1 | 规则裁决与生成表达必须分层 | 客户要求保留问卷和决策树，同时要求千问真正参与；混在一起会变成 AI 自主诊断 | 证型、分期、穴位、疗法和禁忌只来自确定性规则；千问只接收已裁决参数与已审核资料 |
| 2 | 问卷后必须形成可感知的动态生成与连续追问 | 客户明确把“固定模板、不能追问”视为当前未解决问题 | 结果不是固定模板原样输出；同一会话可围绕已判方案追问，并能引用有依据的文字或已审核视频 |
| 3 | 医学越界必须由系统验证和降级，不只依赖 Prompt | 实验已经复现模型自创次数、时长和操作方式；提示词本身不是确定性边界 | 名单外穴位/疗法以及无依据次数、时长、力度零出现；资料不足、冲突和模型失败时回退安全文本 |

## 二、知道了它们，我要作的是哪一个决定

> 决定是否先冻结一份可验收的客户需求口径，再只实现“规则结果 → 千问转译 → 方案后追问 → 安全降级”这一条最小闭环。

## 三、哪些看起来相关，但知道了也不改变这个决定

**排除项。一条不看的，抵十条要看的。**

- 不比较通用 Agent 框架，也不重做决策树；二者都不改变客户当前是否认可交互闭环。
- 不承诺模型生成视频；客户截图表达的是“回答可带视频”的体验，交付只引用后台已审核视频。
- 不新增轻症/重症临床判定；现行 truth 只有急性期/缓解期，缺失规则时不让模型补猜。
- 不先升级向量数据库、重排器或视觉识别；这些不是验证主链路的前置条件。

## 四、我的预判

**不必假装客观。** 写下你现在的猜测，让 AI 回源码去推翻它。

| 我怀疑 | 请核实 |
|---|---|
| 分析基线 `main@0a2c312` 已有规则判定、模型调用和知识问答，但模型解释仍被压回固定模板 | 核查该基线的 `conversation-service.ts:342-390`、`output-validation.ts:223-277`、`deepseek-model-adapter.ts:10-11` |
| 分析基线没有把方案后的追问绑定到当次规则结果和方案白名单 | 核查该基线的会话服务、知识问答入口与模型端口；找不到就明确记为“没有” |
| 只改 Prompt 无法保证医学安全 | 用 `docs/experiments/001_zhenyiwen-constrained-dialogue.md:41-50` 的 F1–F3 证伪 |

> 通行建议是隐藏立场以免诱导模型。这里反过来——**亮出立场，但把任务改造成可证伪的核验。**

## 核查结果

三条预判均获得证据：前两条描述了分析基线的真实差距，第三条被实验 F1/F2 直接证实。
作者随后选择直接开发，前两项差距已在当前工作树补齐；这不改变当时判断，只改变后续状态。

---

**放行**：三条命门都填了、都能被「有 / 部分 / 没有」回答，就可以开始取材。

# 决策点 · 模型会不会乱说，以及后端医学校验是否必要

> 2026-08-21。**这一份是给 AI 用的裁刀，不是给你答的题。**
> 先写下不可妥协的几条，让候选逐格回答；**允许大量功能根本不读**。
> ⚠️ 三条草稿由 AI 先填，**你只改这三条**。改完再往下走。

## 我要作的决定

模型会不会乱说，以及后端医学校验是否必要

## 一、这件事上，我不可妥协的是哪几条

逐条编号。每条要能被「原生支持 / 部分 / 没有」回答，答不了的说明写得太虚。

| # | 不可妥协的 | 为什么它是命门 | 怎么算满足 |
|---|---|---|---|
| 1 | 患者可见回答不能把模型生成的高风险医学动作当成已批准事实 | 一次具体参数、禁忌或诊断越界就可能造成真实伤害 | 已知的诊断、方案、穴位/疗法、次数、时长、力度、剂量、禁忌和疗效边界有确定性阻断或证据门禁 |
| 2 | 对话理解必须保持开放，硬边界只限制医学权威和外部副作用 | 如果代码把每句话都管死，系统会回到“没关键词就拒答” | 普通解释仍由模型自然组织；后端只检查高风险 invariant、证据用途和结构化输出 |
| 3 | 必须区分“模型建议”与“系统批准” | Pi/DSH 的工具边界不能直接证明医疗回答安全，检索到资料也不等于获得使用许可 | 模型可以决定是否搜索和如何表达，但后端决定证据能否进入患者回答、结果能否持久化/流式发送 |

## 二、知道了它们，我要作的是哪一个决定

> （一句话。答不出来，说明上面写的不是命门，是好奇。）

在不牺牲自然对话的前提下，选择患者端模型输出的最小硬边界：哪些内容由模型负责理解和表达，哪些内容必须由后端确定性阻断或要求批准证据。

## 三、哪些看起来相关，但知道了也不改变这个决定

**排除项。一条不看的，抵十条要看的。**

- Pi/DSH 的工具权限、沙盒、审批、会话重建和参数校验；它们是否有医疗 claim 级回答校验并不改变我们是否需要自己的医学边界。

## 四、我的预判

**不必假装客观。** 写下你现在的猜测，让 AI 回源码去推翻它。

| 我怀疑 | 请核实 |
|---|---|
| 我怀疑 Pi 没有通用权限边界，主要依赖宿主环境；它也没有医疗回答校验 | Pi `README.md:38-46,502` 明确写明无内置权限系统、默认继承启动进程权限、无权限弹窗；`agent-loop.ts:616-647` 是工具参数/工具调用前拦截，不是助手文本医学校验 |
| 我怀疑 DSH 有后端硬边界，但边界针对工具副作用和持久化，不针对医学 prose | DSH `user-approval/src/index.ts:84-102,239-250`、`sandbox-policy/src/index.ts:37-45,112-140`、`session-checkpoint-policy/src/index.ts:52-82`；核对的 Agent loop 没发现 medical claim validator |
| 我怀疑我们不能从“Pi/DSH 没做医疗校验”推出“我们也不需要” | 实验 002 的模型直出人工复核：06、11、12 出现 P1 医学越界；实验 004 回放将同一输出通过小门禁后拦截 3/3 |

> 通行建议是隐藏立场以免诱导模型。这里反过来——**亮出立场，但把任务改造成可证伪的核验。**

---

**放行**：三条命门都填了、都能被「有 / 部分 / 没有」回答，就可以开始取材。

# 决策点 · 既能广泛回答又不乱说的医疗 Agent 架构

> 2026-08-21。**这一份是给 AI 用的裁刀，不是给你答的题。**
> 先写下不可妥协的几条，让候选逐格回答；**允许大量功能根本不读**。
> ⚠️ 三条草稿由 AI 先填，**你只改这三条**。改完再往下走。

## 我要作的决定

既能广泛回答又不乱说的医疗 Agent 架构

## 一、这件事上，我不可妥协的是哪几条

逐条编号。每条要能被「原生支持 / 部分 / 没有」回答，答不了的说明写得太虚。

| # | 不可妥协的 | 为什么它是命门 | 怎么算满足 |
|---|---|---|---|
| 1 | 回答能力优先，风险处理不能把大多数问题变成拒答 | 产品价值来自理解、追问、补证据和回答，不是拦截率 | 普通问题保持单次自然回答；风险回答优先局部修复或关键追问 |
| 2 | 高风险医学 claim 必须能走回证据和明确判据 | 单纯相信生成模型或同模型复核会继承同类错误 | claim 有来源、用途和风险结构；独立判据能在定标集上识别已知风险 |
| 3 | 额外安全成本必须按风险分配 | 每条都串行调用审阅模型会直接破坏对话体验 | 低风险无额外模型调用；高风险路径记录 P50/P95、超时和降级 |

## 二、知道了它们，我要作的是哪一个决定

选择“关键词拦截、同模型复核、独立审阅修复、自适应混合”中的首选架构，并确定开放医疗
问答的首期知识与责任范围。

## 三、哪些看起来相关，但知道了也不改变这个决定

**排除项。一条不看的，抵十条要看的。**

- 不比较某个大模型的综合榜单名次；这次决定的是系统如何分配证据、审阅、修复和延迟预算。

## 四、我的预判

**不必假装客观。** 写下你现在的猜测，让 AI 回源码去推翻它。

| 我怀疑 | 请核实 |
|---|---|
| 关键词规则只能做最后的便宜保险，不能承担回答质量 | 用实验 004/005 的未知错误、误判和修复结果证伪 |
| 同一个生成模型再审一遍会有相关性错误 | 实验 005 不给审阅器人工标签，核对其对已知 P1 的识别率 |
| 更可行的是“证据化 claim + 独立审阅 + 定向修复”，并且只在风险路径启用 | 核对 NeMo Guardrails、Guardrails AI、RAGChecker 与医疗 RAG 的原生边界 |

> 通行建议是隐藏立场以免诱导模型。这里反过来——**亮出立场，但把任务改造成可证伪的核验。**

---

**放行**：三条命门都填了、都能被「有 / 部分 / 没有」回答，就可以开始取材。

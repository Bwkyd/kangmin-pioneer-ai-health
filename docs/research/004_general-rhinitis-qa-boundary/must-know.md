# 决策点 · 通用鼻炎科普问答应采用哪种知识边界与检索降级策略

> 2026-08-17。**这一份是给 AI 用的裁刀，不是给你答的题。**
> 先写下不可妥协的几条，让候选逐格回答；**允许大量功能根本不读**。
> ⚠️ 三条草稿由 AI 先填，**你只改这三条**。改完再往下走。

## 我要作的决定

通用鼻炎科普问答应采用哪种知识边界与检索降级策略

## 一、这件事上，我不可妥协的是哪几条

逐条编号。每条要能被「原生支持 / 部分 / 没有」回答，答不了的说明写得太虚。

| # | 不可妥协的 | 为什么它是命门 | 怎么算满足 |
|---|---|---|---|
| 1 | 患者可见医学事实必须可追溯，模型不得自行诊断，也不得借“通用知识”改变证型、分期或方案 | 项目红线要求规则裁决与模型表达分离；无来源的流畅答案会直接破坏可信内容和医学安全闭环 | 每条实质性科普回答列出已启用来源；无足够依据时明确降级；证型、分期和方案仍只读确定性结果 |
| 2 | 客户高频的通用科普问题不能因为“不是当前方案穴位”而整体拒绝 | 客户已用“经常鼻子痒是为什么”复现可用性失败；只会答四个穴位不构成患者科普问答型智能体 | 同一问助手入口能区分通用科普、当前方案追问和越界医疗请求；一组客户式问法达到书面覆盖门槛 |
| 3 | 优先复用现有后台、知识问答和安全校验，不把内容不足误判为重做后台 | 报价有限，正式云资源待尾款；后台已具备资料上传、索引、启停和检索测试 | 第一切片无需重建后台或新增正式云服务；只接通必要路由、补受控资料并用有界实验判断是否升级检索 |

## 二、知道了它们，我要作的是哪一个决定

> 决定通用鼻炎科普采用严格“有来源才回答”，还是允许千问在无检索命中时使用模型自有
> 医学知识；并决定它如何进入现有问助手流程。

## 三、哪些看起来相关，但知道了也不改变这个决定

**排除项。一条不看的，抵十条要看的。**

- 不比较新的通用 Agent 框架；现有 TypeScript 会话、规则和持久化链路无需迁移。
- 不提前决定 PostgreSQL、COS、微信审核或正式小程序发布时间，它们不改变本轮问答边界。
- 不研究复杂统计、模型参数中心或调理方案后台编辑，它们不在本轮报价与客户反馈命门上。
- 不把客户 PPT 的宣传表述直接当作医学事实或系统已实现能力。

## 四、我的预判

**不必假装客观。** 写下你现在的猜测，让 AI 回源码去推翻它。

| 我怀疑 | 请核实 |
|---|---|
| 当前失败不是“没接千问”，而是知识供给太窄且方案后追问只在点名穴位时检索 | 回源码、线上状态与客户问法确认 |
| 管理后台核心功能已经存在，继续做后台页面不能直接解决“鼻子为什么痒”被拒绝 | 回管理 Web、浏览器 E2E 与报价逐项确认 |
| 立即开放模型自有医学知识能提升覆盖，但会失去来源闭环并扩大幻觉风险 | 用官方千问 RAG 配置、WHO/NIST 风险资料和 RAG 原始论文证伪或确认 |

> 通行建议是隐藏立场以免诱导模型。这里反过来——**亮出立场，但把任务改造成可证伪的核验。**

---

**放行**：三条命门都填了、都能被「有 / 部分 / 没有」回答，就可以开始取材。

# 决策点 · 修正版患者智能体实验是否同时加入上下文与模型变量

> 2026-08-22。**这一份是给 AI 用的裁刀，不是给你答的题。**
> 先写下不可妥协的几条，让候选逐格回答；**允许大量功能根本不读**。
> ⚠️ 三条草稿由 AI 先填，**你只改这三条**。改完再往下走。

## 我要作的决定

修正版患者智能体实验是否同时加入上下文与模型变量

## 一、这件事上，我不可妥协的是哪几条

逐条编号。每条要能被「原生支持 / 部分 / 没有」回答，答不了的说明写得太虚。

| # | 不可妥协的 | 为什么它是命门 | 怎么算满足 |
|---|---|---|---|
| 1 | 上下文与模型必须正交比较 | 否则无法知道改善来自知识组织还是模型能力 | 同一题、提示词、采样配置下，每个模型跑同一组上下文管线 |
| 2 | 同时奖励“该说”与“该克制” | 只放拒答题会把僵硬误判为安全 | 黄金集包含一般科普、truth 授权具体项、专业原文不可转用途、急症/越权四类 |
| 3 | 只评价书面判据能支持的结论 | 行为通过不等于医学正确或临床批准 | 参考行为运行前写定；输出盲化复核；医学未定标项单列，不进入总安全声明 |

## 二、知道了它们，我要作的是哪一个决定

是否先用“上下文管线 × 候选模型”小型析因实验筛出可行组合，再决定 Agent 自主搜索与生产实现。

## 三、哪些看起来相关，但知道了也不改变这个决定

**排除项。一条不看的，抵十条要看的。**

- 不比较综合榜单、代码能力或百万上下文长度；它们不能替代本项目中文患者问答实测。
- 不在本轮测试图片理解；文字安全问题与视觉能力分开，避免混淆变量。
- 不建设运行时第二模型审批或关键词拦截器。

## 四、我的预判

**不必假装客观。** 写下你现在的猜测，让 AI 回源码去推翻它。

| 我怀疑 | 请核实 |
|---|---|
| 007 只证明“专业原文 + 警告卡”无效，没有否定上游患者知识选择 | 用修正版上下文管线与同一模型对照 |
| `deepseek-v4-flash` 可公开调用，但 `deepseek-v4-flash-vision-exp` 可能不是公开模型 | 先查官方文档，再用账号 `/models` 回读，不把 404 当能力失败 |
| 模型与上下文可能存在交互，单独先后比较会漏掉 | 完成同一小型全矩阵，再只对候选组合做多次稳定性试次 |

> 通行建议是隐藏立场以免诱导模型。这里反过来——**亮出立场，但把任务改造成可证伪的核验。**

---

**放行**：三条命门都填了、都能被「有 / 部分 / 没有」回答，就可以开始取材。

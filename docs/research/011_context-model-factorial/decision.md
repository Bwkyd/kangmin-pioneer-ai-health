# 取舍卡 · 修正版患者智能体实验是否同时加入上下文与模型变量

> 2026-08-22。**这一步的最终产物。一页纸。**
> ⚠️ 最后一栏「需要你拍板的地方」**AI 不许代填**——草稿归它，拍板归你。

## 这次不可妥协的几条

1. 上下文管线和模型正交比较，不混淆变量。
2. 同时测普通回答、授权具体回答和无授权克制，不奖励一味拒答。
3. 参考行为先写、输出盲化复核，医学未定标不包装成安全结论。

## 我选择什么

先做实验 008 的小型全矩阵：`qwen3.7-flash` 与官方可用 DeepSeek 候选共同比较无上下文、
原始专业检索、原文警告卡、只含 truth 授权患者知识四条管线；一次筛选后才对候选组合做
多次稳定性试次。

## 我放弃什么

放弃用 007 否定上下文工程、放弃马上只做模型横评、放弃把未核实的 `vision-exp` 写成已
支持，也暂不把 Agent 自主搜索和回答后审核混入本轮。

## 依据来自哪一类

- [x] 自有积累 —— 实验 003/006/007 与 truth；能复现当前模型、检索和授权范围
- [x] 他人积累 —— 两篇方法文章与 DeepSeek 官方 API 文档；只借方法和接口事实
- [x] 自己跑的 —— 实验 008 的 240 条全矩阵与高风险人工校准

---

## 拿两把尺子量一遍

### 一、复用分级：能用到什么程度

| 来源 | 权限 | 这次实际用到哪一步 |
|---|---|---|
| **自家的** | 可照搬，先确认权利与时效 | 复用语义向量、原始失败和 truth 授权文字，重写实验 |
| **外部的** | 只读借鉴——读懂再自己写，先查协议 | 用评测原则设计黄金集，用官方文档配置模型，不复制实现 |

> 传统分析问的是「信谁」。**这里多一问：我能用到什么程度。**
> AI 一秒钟就能把看过的东西复制出来——**「能不能抄」不再受能力限制，只受纪律限制。**

⚠️ 交付前回来一趟：拿参考区的原件比对自己的产出，**查有没有抄**。

### 二、满意与最优：什么时候够

| | |
|---|---|
| **最优** | 把所有选项都比一遍，选最好的那个 |
| **满意** | 搜到够好就停 |

> 人并不像贪婪搜索那样穷尽所有选项，**搜到够好就停**——这是人省算力的办法，
> 也该是你做分析的办法。
>
> **AI 不累，能一直查下去。不给它一条停止线，它就替你把「最优」当成目标。**

完成 20 条首轮矩阵后，患者知识选择已明显优于两条专业原文管线，但三个模型均未通过
高风险人工校准；已淘汰上下文管线并按停止线不追加多次试次。

## 支持它的最强依据

007 的四组在普通题均能回答，却都在多个动作题失败，说明既要保留有用性也要改变专业资料
进入模型的方式；DeepSeek 官方同时提供 V4 Flash 文本 API，加入模型轴的实现成本低。

## 反对它的最强依据

20 条仍是小样本；truth 只确认方案名称与部分执行频率，没有提供所有医学问题的金标准。
模型全矩阵会增加调用量，却仍不能证明临床安全。

## 仍未补齐的缺口

作者需要复核 20 条参考行为是否符合产品口味；医学因果与禁忌仍只能标“待医学确认”。

## ⚠️ 需要你拍板的地方

**这一栏填不满，分析就还没做完。**

| # | 需要拍板的 | 选项 | 我的建议 | 拍了吗 |
|---|---|---|---|---|
| 1 | 是否把模型轴加入修正版实验 | A 只修上下文 / B 同步做小型全矩阵 | **B**，同题正交比较后再扩大 | ☑ 作者已确认 |
| 2 | `vision-exp` 不在官方列表时怎么办 | A 记不可用 / B 猜测接口继续请求 | **A**；先用账号 `/models`，不存在就不计为模型失败 | ☑ 账号已列出并完成文本实验；未验证视觉 |

## 实验改变了什么

- 选择“患者知识选择”作为下一版上下文主路线，淘汰专业 Top-k 直注入和原文警告卡；
- 不选择生产模型：千问自动行为分与成本占优，但仍有 4/10 明确高风险失败；
- 下一件事改为修评测器与医学定标，不继续堆提示词、不增加运行时同类模型审批。

---

## 停，还是再补一个

**判据**：能列出上面那些拍板点，就够了。列不出来，是还没读懂。

停不下来时的自检：**关掉搜索**，写下我选什么、我舍什么，
再找一条**对这个选择最不利**的依据攻击它——选择不变、命门无空白，就停。

---

## 收尾三件

1. **判断落回真相源**：这张卡上确认下来的结论，整理进 `vault/truth/`，句句带出处。
   材料留在参考区（可重建，不进仓），**你的判断必须进仓**。
2. **交付前回一趟参考区**：拿它比对，查自己有没有抄。
   **同一份材料，开工前是老师，交付前是尺子。**
3. **放行**：新开一个空会话，只喂这一份，问三句——选了什么？舍了什么？哪里要我拍板？答得齐就过。

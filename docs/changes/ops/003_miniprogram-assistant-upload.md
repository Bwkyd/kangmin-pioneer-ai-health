---
type: ops
number: "003"
date: 2026-08-24
title: 小程序问助手开发版上传与交付收尾
tags: [wechat, miniprogram, assistant, upload, verification]
related: ["ops/002"]
---

# ops/003 小程序问助手开发版上传与交付收尾

## 执行概述

- 时间：2026-08-24
- 执行人：Codex；作者已明确授权提交、CI、合并、部署与清理
- 代码基线：PR #263 squash 合并为 `main@c06a02c`
- 环境：macOS、微信开发者工具 RC 2.02.2608031
- 结果：开发版 `0.1.2` 上传成功，总包体 405,257 字节

## 合并与验证

提交前 `cd src && npm run check` 通过：414 项测试中 338 通过、76 项因未配置 PostgreSQL/S3
按约定跳过、0 失败，患者端与管理端真实 Chrome E2E 均为 PASS。PR #263 的 `quality`
（3 分 08 秒）和 `image`（44 秒）成功后 squash 合并。`km-review` 从患者可懂性、工程事实和
医学安全三视角复核，P0、P1、P2 均为 0。

## 隔离上传

从合并后的 `src/miniprogram` 创建 `/tmp` 临时副本，只在副本的 `project.config.json` 注入
已确认 AppID；仓库工程 AppID 继续留空，未写入 AppSecret、患者数据或其他凭据。上传前
微信开发者工具 CLI 回读 `{"login":true}`。

CLI 使用版本 `0.1.2`、描述“小程序问助手交互优化”上传，返回 `upload` 成功；信息文件记录
总包体 405,257 字节。上传副本和本轮开发工具预览副本随后移入 Mac 废纸篓，可恢复；CLI
服务端口 49721 退出后无监听。

## 服务复核与边界

本轮改动只作用于原生微信小程序，没有服务端运行时差异，因此未创建或切换 Web release。
公网 `https://140.143.120.176` 的首页、`/admin` 和 `/live` 本轮实测均为 200；`/ready`
仍为既有 503。当前 Mac 没有可被目标主机接受的 SSH 密钥，本轮没有重做 systemd、SQLite
`quick_check` 或 release 指向的服务器内部读取，不能把历史结果冒充本轮实测。

开发版上传只证明微信接口接受代码，不等于真机视觉、真实微信登录、合法域名、体验成员可见、
审核通过、正式发布、临床批准或客户验收。

仓库只配置 GitHub `origin`。CNB CLI 登录有效，但按 `kangmin` 和 `pioneer` 查询可见仓库均为
0，因此没有本项目的 CNB 分支可验证或删除；未向猜测的 CNB 仓库写入。

## 后续 TODO

- [ ] 配置正式微信身份链路与合法域名后验证登录及失败路径
- [ ] 由体验成员在真机核对问助手布局、动画、键盘发送和完整问卷
- [ ] 客户确认后再进入提审与正式发布
- [ ] 需要服务器内部复核时，先恢复受控 SSH 密钥再读取，不使用口令试探或历史状态替代

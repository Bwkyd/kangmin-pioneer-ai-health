# 007 修正评估页面与有序六步证型决策树

- 状态：已合并交付，客户已收到反馈
- 日期：2026-08-09
- 代码基线：`9e1e75e`；当前线上：`e87151d`；业务合并：`c621704`（PR #188）；收尾记录：`265d00c`（PR #189）
- 页面依据：`vault/truth/product/assessment-page-content.md`
- 规则依据：`vault/truth/clinical/assessment-rules.md`、`vault/truth/clinical/syndrome-six-step-decision-tree.md`

## 目标

页面只按客户《页面展示》呈现 Q1–Q14，不再插入作者追加的安全、确诊、年龄、
鉴别和严重度题。Q1–Q11 收齐后按严格有序六步树判证型；需要继续时，第 5/6 步
作为 Q8/Q10 独立二次确认节点；随后必须完成 Q12–Q14 才组合输出结果。CLI、HTTP
和 Web 复用同一状态机与同一份题库。

## 非目标

- 不修改客户 raw 原件；页面和规则分别由对应 truth 副本承担验收依据；
- 不修改客户提供的方案正文、方法选择关系或医学禁忌；
- 不把证型人群占比写成诊断配额；“肾阳不足约 10%”只用于上线后异常监测；
- 不允许模型根据自由文本直接决定跳转或证型。

## 依据与优先级

1. `vault/truth/product/assessment-page-content.md` 唯一定义 Q1–Q14 题面、选项、顺序和结果表达；
2. truth 六步树负责证型主判定；
3. `vault/truth/clinical/assessment-rules.md` 负责两层顺序、证型跳转、期别与组合输出；
4. `vault/raw/clinical/急性期方案、调体方案（缓解期方案）.md` 负责叶节点后的可选方法；
5. v3 七规则和现有 benchmark 仅作反向回归证据，不得覆盖客户确认规则。

## 已确认缺陷

- 现有实现把 Q8/Q9/Q10 的 B 错误压成 `yes`，导致 Q10B 提前判肺经伏热、Q8B
  提前判脾气虚弱、Q9B 错判肾阳不足；
- 规则引擎并行计算 T1–T5，无法表达“上一节点未命中才进入下一节点”；
- 原实现以同一症状字段保存重复题，不能同时保存首次回答和二次确认；
- 当前 12 场景 benchmark 以错误的 v3 规则生成期望值，测试全绿不能证明符合客户树。

## 实施设计

### 1. 版本与节点身份

发布 `clinical-rules-v3`。Q1–Q14 按原题号保存原始选项；树前四步从 Q10/Q8/Q6/Q9
确定性映射，第 5/6 步使用 `step5_q8_confirm`、`step6_q10_confirm` 独立留痕，
不再复用首次答案或压成症状布尔值。

### 2. 唯一确定性状态机

在应用服务下建立单一状态机，输入当前节点与节点答案，输出下一节点或叶子证型。
Q1–Q11 必须先逐题完成，再执行树；树命中证型后仍按“两层缺一不可”继续 Q12–Q14，
最终才结束会话。CLI、HTTP、Web 全部复用该服务；DeepSeek 只能把自然语言提取为受控选项候选，
经患者确认后才提交状态机。

规范跳转：

| 节点 | A | B | C |
| --- | --- | --- | --- |
| `step1_q10` | 肺经伏热 | `step2_q8` | `step2_q8` |
| `step2_q8` | 脾气虚弱 | `step3_q6` | `step3_q6` |
| `step3_q6` | `step4_q9` | `step4_q9` | `step4_q9` |
| `step4_q9` | `step5_q8_confirm` | 肺气虚寒 | 肺气虚寒 |
| `step5_q8_confirm` | 肾阳不足 | `step6_q10_confirm` | `step6_q10_confirm` |
| `step6_q10_confirm` | 寒热错杂、虚实并见 | 肾阳不足 | 肾阳不足 |

Q1/Q2/Q5/Q7/Q11 只生成辅助印证信息，不得覆盖叶子结果。

### 3. 持久化与兼容

- 复核后确认现有 SQLite、PostgreSQL 表的主键均为 `(session_id, field_code)`；以稳定
  节点身份作为 `field_code` 即可独立保存六个答案，无须冒险重建数据表；
- 会话详情、历史恢复和决策凭证返回节点身份、中文答案及规则版本；
- 新规则发布后不续跑旧规则未完成会话：首次发现即原子标记 `abandoned`，页面锁定并
  只提示一次新建评估；失败选项不提前追加到聊天历史，杜绝重复“不清楚”；
- unknown 不等于任一医学选项，保持当前节点并进入澄清，不允许静默放行。

### 4. 实施顺序

1. 先写六步状态机及 111 路径穷举测试；
2. 改答案模型与仓储写入，核验 SQLite 实存和 PostgreSQL 契约；
3. 让会话服务、CLI/HTTP/Web 共用状态机并修复历史恢复；
4. 重写 benchmark 期望来源，补关键错误路径和 unknown；
5. 完成全量检查、浏览器 E2E、备份与试用环境部署核验。

## 验收标准

- 页面第一题必须是 Q1，严格推进到 Q14，不得出现安全/确诊/年龄/鉴别/严重度插入题；
- 穷举全部 **111 条根到叶 A/B/C 路径**，逐条验证题序、跳转、未进入节点不出现、
  叶节点立即终止、二次确认独立留痕及五类结果；
- 重点回归：Q10A 首步立即肺经伏热；Q10B/C 不提前结束；Q8B/C 不提前判脾虚；
  Q9B/C 必为肺气虚寒；
- Q1/Q2/Q5/Q7/Q11 的任意辅助答案不能改变已经确定的叶子证型；
- CLI、HTTP、Web 对相同节点答案序列输出一致；刷新、切换历史后节点、中文答案和
  结果不变；
- 旧版本未完成会话不能混入新规则继续，且失败答案不进入患者消息历史；
- SQLite 实存测试与 PostgreSQL 仓储契约测试一致通过；
- `npm run check`、`python3 scripts/structure-lint.py .`、`git diff --check` 和浏览器
  E2E 全部通过后，才允许部署。

## 风险与限制

- 《页面展示》没有年龄题，当前客户试用页面按成人方案装配；若后续需要儿童方案，必须
  先由客户补充页面题目与分流口径，不能再次自行加题；
- 现有 v3 测试可能把错误行为固定为期望值，需逐项按新 truth 重写，不能机械改绿；
- 本计划只修证型主树，急性/缓解期等仍按当前独立规则运行，其医学口径需另行确认。

## 第一轮收尾证据（`eba184f`）

- 新增唯一状态机 `syndrome-decision-tree.ts`，规则包升级为
  `clinical-rules-v2`，旧七条证型规则退出活动规则包；
- 自动穷举 111 条根到叶路径，并覆盖 Q10A、Q8A、Q9B/C、重复 Q8/Q10、辅助事实
  不覆盖、非法布尔/非 A–C fail-closed；
- 会话层按六个节点分别留存原始 A/B/C，SQLite 已验证独立实存；PostgreSQL 契约
  已补充，未配置 `KANGMIN_TEST_DATABASE_URL` 时按项目惯例跳过真实 PG 集成；
- 旧规则包的未完成会话返回 `protocol_incompatible`，不得混用新规则续跑；
- CLI/HTTP 复用同一应用服务；浏览器 E2E 已验证
  Q10B → Q8C → Q6B → Q9B 收口为肺气虚寒并保留原题选项；刷新后再次进入
  问助手会恢复历史并自动定位到最新结果；
- 本地浏览器 E2E、完整评估 smoke、12/12 CLI benchmark、全量 `npm run check`、
  `python3 scripts/structure-lint.py .` 与 `git diff --check` 均已通过；
- 已推送 `agent/fix-six-step-tree`，PR #188 已 squash 合并为 `c621704`；客户试用环境已部署
  `eba184f`。部署前完成独立端口临时库预检，生产 SQLite 已备份，公网 E2E、
  服务日志及静态资源 SHA-256 均核验通过；合并代码树与线上业务代码一致，作者已反馈客户。

## 客户复测后的 v3 修复证据

- 新增共享题库 `assessment-questionnaire.ts`，前端和临床内核不再各维护一份题面；
- 生产规则包升级为 `clinical-rules-v3`：Q1–Q11 → 六步树/二次确认 → Q12–Q14；
- 旧会话规则不兼容时由服务端落为 `abandoned`，Web 不再乐观追加失败选项；
- 生产 E2E 覆盖 Q1–Q14、寒热错杂二次确认、分期、刷新恢复，以及截图中的旧会话
  重复点击回归；`npm run check` 全绿（333 tests：257 pass、76 跳过、0 fail），
  浏览器 E2E、9/9 CLI benchmark、完整 smoke 均通过；提交 `e87151d` 已推送并部署。
  线上独立预检、停服 SQLite 备份、原子 release 切换、公网 Q1–Q14 HTTP E2E、
  静态资源 SHA-256 与服务状态均已核验；旧 release 保留可回滚。

## 最终交付自检（`main@265d00c`）

- PR #188、#189 均已合并，quality 与 image CI 均通过；本地 `main` 与
  `origin/main` 同步，任务分支已清理；
- 本次从规范入口 `src/` 重新执行 `npm run check`：333 tests、257 pass、76 项因未配置
  PG/S3 跳过、0 fail，浏览器 E2E PASS；结构检查、diff 检查及公网 `/live` 同步通过；
- 页面、前置规则、六步树 truth 及对应 raw 来源链完整，014 计划与计划索引状态一致；
- 共享题库、`clinical-rules-v3`、独立二次确认、旧会话隔离和逐方法展示均能在实现与
  测试中定位，不存在只写文档未落代码的事项；
- 线上 `e87151d` 的业务代码树与合并提交 `c621704` 一致，PR #189 仅更新状态文档，
  无需再次部署；作者已向客户反馈；
- 本任务无遗留。儿童分流因客户页面没有年龄题，继续作为资料限制保留，须待客户补充
  题目和口径后另开任务，不在本计划内擅自实现。

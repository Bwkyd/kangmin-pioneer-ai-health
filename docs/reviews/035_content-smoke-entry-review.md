# #333 内容供给闭环快速冒烟入口评审

- 基线：`issue-333-content-smoke`，源自 `origin/main@78c17db`
- 对象：`src/scripts/run-content-smoke.mjs`、`src/package.json`、实验与状态记录
- 方法：`km-review` 患者可懂性、工程事实、医学安全三视角；结合正向门禁与有界负向实验

## 患者可懂性视角

- P0–P2：未发现。
- 本轮只增加开发者测试入口，不改变患者页面、患者文案或接口载荷；不会把内部
  TAP、选择器失败信息暴露给患者。

## 工程事实视角

- P0–P2：未发现。
- 入口先构建，再对两个既有内容行为分别启动 Node 测试进程；逐字匹配且要求唯一
  实际执行的非跳过测试。文件缺失、目标名漂移、零/部分命中和子进程失败都会退出非 0。
- 正向 `2/2` 通过；测试名漂移实验 exit 1；把启用断言改坏的失败传播实验 exit 1；
  恢复生成产物后入口再次通过。旧 `test:smoke:core` 仍 2/2，完整 `npm run check`
  与真实 Chromium E2E 通过。
- 本地 PostgreSQL、S3/MinIO 未配置，相关完整门禁按约定跳过；不把跳过写成通过。

## 医学安全视角

- P0–P2：未发现。
- 测试只复用既有知识登记、索引、启停和检索契约；没有新增证型、分期、方案或医学
  参数裁决。患者问答仍由既有测试中的受控回答端口提供，患者侧不把 Top-k 候选冒充
  已采用来源。

## 结论

没有需要阻断合并的 P0–P2。P3 也不新增：本轮保持最小编排，不引入重复 fixture、
生产依赖或运行时部署；后续若 Node TAP 输出契约变化，目标选择器应随 CI 失败显式修正。

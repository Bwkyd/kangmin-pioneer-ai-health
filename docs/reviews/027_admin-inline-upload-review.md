# 管理后台任务内上传元反思与交付前复核

> 日期：2026-08-26
> 基线：`main@31c7e9f` 加 `codex/issue-279-inline-uploads` 当前实现。
> 对象：一级导航、文章图片/附件、视频文件/封面和上传失败重试。

## 患者可懂性视角

本轮只改变运营人员上传文件的入口和状态反馈。文章封面、正文图片、视频与视频封面仍通过
既有患者预览和发布规则进入患者端，没有修改患者文案、排序或可见条件。未发现 P0–P2 问题。

## 工程事实视角

侧栏不再显示一级素材库；底层 `media` section、统一存储、上传会话、哈希幂等和既有引用均
保留。文章和视频表单显示正在上传、成功文件名或失败重试提示，服务端具体失败原因仍在当前
页面 toast 展示。

sequential-thinking 六步发现首版为突出正文图片而误删 PDF、Word、Markdown、TXT 附件接受
能力这一 P2，已恢复原类型推断和链接插入，并增加附件接受范围回归。真实浏览器 E2E 从文章
表单完成正文图、封面失败与同文件重试，从视频表单完成视频和封面上传，不再依赖一级素材页；
患者预览分别验证封面、正文图和视频。修复后 P0–P2 清零。

## 医学安全视角

本轮没有改变文件类型/大小服务端校验、内容发布校验、知识启用门禁、问卷、证型、分期、方案
或临床 truth。未发现 P0–P2 问题。

## 已知边界

#279 仍需后续补齐二级文件管理、引用去向与被引用文件删除保护的运营界面；AI 知识的连续闭环
由 #276 子轮实现。因此本轮不能单独关闭 #279。

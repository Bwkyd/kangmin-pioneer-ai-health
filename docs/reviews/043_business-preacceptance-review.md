# 四条业务链技术预验收复核

- 状态：P0、P1、P2 清零；PR CI 与合并完成
- 日期：2026-08-27
- 基线：`origin/main@7594dc6` 加 `codex/business-preacceptance` 候选
- 对象：预验收脚本、CI、依赖锁、计划与实验记录

## sequential-thinking 元反思

六步复核了目标偏移、真实入口、正反路径、测试假绿、旧功能破坏和外部验收边界。实现只
建立验证入口和门禁，没有把测试工作扩成业务重写。TAP 必须逐字且唯一命中，进程退出码、
零命中与多命中共同失败；完整回归覆盖同类错误变式。

## `$km-review` 三视角

### 患者可懂性

未修改患者文案或界面；验收记录明确区分技术预验收、真机、客户验收和医学批准，未发现
误导患者或暴露内部载荷的问题。P0–P3 均未发现。

### 工程事实

真实管理 CLI、HTTP、SQLite、小程序页面和 Chromium 均有可回读证据；脚本不会把筛选漂移
当作通过。评审发现的 `nanoid@3.3.16` 高危开发依赖已最小升级并在干净安装后审计为 0。
CI 保留 PostgreSQL、MinIO/S3、OCI，并新增四组预验收步骤。最终 P0–P2 为 0。

### 医学安全

没有修改题面、证型、分期、方案、提示词、知识正文或模型裁决；患者正反路径仍验证药量
前置阻断、模型失败不泄露切片、unknown 与未授权 fail-closed。未发现模型越权或新增医学
内容。P0–P3 均未发现。

## 结论与限制

候选可以进入 PR CI。微信官方预览只证明当前源码被工具接受；真实微信身份、真机、客户
体验和医学结论仍需作者或客户验证。生产只允许后续做无身份只读复核。

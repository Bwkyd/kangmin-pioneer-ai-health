# 最小分形 workspace 部署后评审

- 基线：`main@ec60f59`，生产 release `workspace-fractal-ec60f59-r2`
- 对象：候选装配、SQLite 备份、systemd 入口、回滚与上线后行为
- 方法：sequential-thinking 五步增量反思；`km-review` 患者可懂性、工程事实、医学安全三视角

## 患者可懂性视角

- P0–P2：未发现。
- 患者端仍为微信小程序，不因旧患者 Web 退役而把管理后台冒充患者入口。生产 `/` 与 `/admin` 均明确显示“管理后台”，真实 Chromium 的标题、正文、网络、console 和 page error 通过。

## 工程事实视角

- 已解决 P1：首个裸机候选携带 macOS 安装的原生依赖，在 Linux 无法加载 canvas。候选门在切换前拦下，正式服务未受影响；第二版改为目标 Linux 按 lockfile 安装生产依赖，审计与原生绑定检查通过，8788 候选真实启动。
- 已解决 P2：实现 PR 新增实验 037、评审 041 后，文档导航仍停在 036/040；本轮修正为包含当前编号并推进下一编号。
- 当前 P0–P2：未发现。新 release、systemd 新入口、active/NRestarts、数据库、内外网与浏览器证据一致；正式切换前有可读备份和原子回滚探针。
- P3：裸机 release 仍依赖服务器执行 `npm ci --omit=dev`。后续若发布频率上升，应让 Linux CI 直接产出带 SHA 的目标平台制品，减少服务器装配步骤；本轮不扩成新发布系统。

## 医学安全视角

- P0–P2：未发现。
- SQL、迁移、truth、问卷、证型、分期、方案、知识发布与急症规则均未修改；正式库 `quick_check=ok`，24 项迁移和全部关键计数切换前后一致。候选只使用在线备份副本。
- `/ready=503` 仍只因既有加密配置；这项配置债务没有被目录任务掩盖或越权修改。

## 结论

部署后 P0、P1、P2 为 0。可以提交运维记录并在 CI 合并后关闭 #350；旧 release 与正式数据库备份继续保留回滚，失败候选和临时制品可安全清理。

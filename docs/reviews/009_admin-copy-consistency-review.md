# 009 · 管理后台运营文案一致性自检

> 状态：已完成；日期：2026-08-18；实现合并：PR #226 / `main@5ffa49d`；部署 release：`admin-copy-cleanup-5ffa49d`；方法：`km-review` 三视角。

## 结论

本轮根据工作台截图继续排查同类问题，确认内部“报价/交付”话术还残留在工作台眉标、主标题、空状态、内容入口卡片以及文章、视频、站内消息、知识库页面眉标。已统一为运营人员可直接理解的“内容运营”“从内容开始”“任务”等语言，并增加跨页面文案回归。

没有发现 P0、P1、P2。改动只涉及管理端可见文案和浏览器断言；没有改路由、权限、服务端命令、状态机、患者端、知识内容或医学规则。

## 三视角结果

### 患者可懂性

未发现 P0–P2。后台不再把内部报价交付链直接暴露给运营人员；文章、视频、消息和知识库入口都说明了处理方向。患者端题面、结果、对话和内容展示未改动。P3：运营人员的长期人工走查仍需作者或客户在真实业务数据下确认，自动化不等于客户验收。

### 工程事实

未发现 P0–P2。`src/web/src/admin/AdminApp.tsx:185-464` 仅调整用户可见字符串；`src/scripts/web-browser-e2e.mjs:686-847` 同时覆盖正向新文案、旧术语缺失和四类页面眉标。完整 `cd src && npm run check` 通过，355 项 Node 测试中 279 项通过、76 项因本机未配置 PostgreSQL/S3 跳过；真实浏览器 E2E、PR #226 的 `quality`/`image` 也通过。结构检查仍只被任务前已有的 `_work/20260814-福建省中医药适宜技术手册-md` 命名问题拦截，未修改作者过程材料。

### 医学安全

未发现 P0–P2。没有新增或修改证型、分期、方案、穴位、疗法、禁忌、次数、时长、模型提示或知识启用规则；本轮不改变知识库参与检索的既有边界。

## 发布与限制

- `main@5ffa49d` 已部署为 `/srv/kangmin-cli/releases/admin-copy-cleanup-5ffa49d`。
- 切换前 SQLite 备份为 `/srv/kangmin-cli/backups/kangmin-mvp-20260818-192108-before-admin-copy-cleanup.sqlite`；切换后服务 active、`NRestarts=0`、`quick_check=ok`，公网 `/live=200`、`/admin=200`，旧 release 保留回滚。
- `/ready=503` 仍是试用环境未配置生产加密密钥的既有限制，不扩大为正式生产就绪或客户验收结论。
- 当前没有可用的图形浏览器实例，本轮不声称完成浏览器视觉目测；本地真实浏览器 E2E 和公网 HTTP/资源核验均有证据。

## 分级

| level | where | what | fix |
| --- | --- | --- | --- |
| P0–P2 | 本轮改动与发布链路 | 未发现阻断问题 | 无需修复 |
| P3 | 真实运营场景 | 尚未由作者/客户完成长期人工走查 | 以真实业务数据走文章发布、视频发布、知识启用三条路径并记录阻塞，不改变医学规则 |

# 最终 workspace 与交付链评审

- 基线：`codex/src-fractal-07`，源自 `origin/main@94ee1ce`
- 对象：最终 workspace 协议、构建清理、CI/Docker 入口与部署停止线
- 方法：sequential-thinking 七步元反思；`km-review` 患者可懂性、工程事实、医学安全三视角

## 患者可懂性视角

- P0–P2：未发现。
- 旧患者 Web 按作者决策退役，患者端仍是微信小程序；根网址与 `/admin` 保持可达同一管理后台。四组行为冒烟与真实浏览器没有发现患者流程或后台入口回退。

## 工程事实视角

- P0–P2：未发现。
- 最终门禁精确检查四个顶层、八个同构 workspace、无迁移白名单、新 CLI/API 入口，并拒绝 CI/Docker/package 恢复旧编译路径。
- 构建清理覆盖根与全部 workspace，陈旧产物负例已实际删除；结构与演化两类门禁均完成正负验证。
- 完整本地测试、真实 Chromium、旧系统回归、生产依赖审计和 SBOM 已通过。本机没有 Docker，真实 PostgreSQL/S3/OCI 仍必须由 PR CI 放行。
- P3：通用演化护栏允许一个低宽度的新 `src` 顶层，而最终 workspace 门禁会拦截；这是通用阈值和业务精确协议的分工，不应合并为一套模糊规则。
- P3：隔离校验脚本曾用 zsh 特殊变量 `path` 覆盖 `PATH`，导致后续命令不可达；实际项目文件未受影响，改用任务专用变量并以绝对命令重跑后结构检查通过。类似脚本必须避开系统变量名。
- 生产 systemd 当前仍指向旧 `dist/http/server.js`。代码合并后若不同时更新为 `apps/kangmin-api/dist/server.js` 会启动失败，因此候选演练、原子更新与回滚是部署硬门，不属于可延后文档项。

## 医学安全视角

- P0–P2：未发现。
- 本轮未改问卷、证型、分期、方案匹配、知识发布、模型提示、急症分流、SQL 或迁移。完整回归仍覆盖 unknown、急症、药量、权限隔离和数据库失败关闭；生产数据库只允许先做在线备份副本演练，不直接实验。

## 结论

本地候选 P0、P1、P2 为 0，可以进入 PR/CI。实现达成最终目录与交付协议，但只有 CI 全绿、候选 release 通过、正式备份与原子切换成功、线上数据库和浏览器复核通过后，才满足 #350；当前不得提前关闭 Issue 或宣称已部署。

# 医学输出拦截选型三视角复核

> 日期：2026-08-23
> 基线：`main@c9b4e4b`，工作区含作者此前未提交的实验与实现改动
> 对象：`docs/research/014_medical-safety-guardrail-selection/`

> 2026-08-23 作者后续取消小模型路线；以下结论已按“薄确定性兜底或完全不拦”重新复核。

## 患者视角

未发现 P0–P2。修订方案明确保留正常科普和无命中自然回答，不向患者展示检索命中或内部风险标签；薄门只处理可直接照做的危险内容。

## 工程视角

### 已修复 P1：输出侧新层可能覆盖模型前硬分流

- `where`：`gap.md` 原级联图、`decision.md` 原建议。
- `what`：初稿只画了候选回答后的筛选，没有画实验 017 已验证的急症与明确高风险模型前分流。
- `why`：若按图实现，急症可能先调用模型，破坏“零模型调用”的既有候选能力，并把安全依赖转回概率生成。
- `fix`：已补输入前确定性分流、零调用回归要求，并在取舍卡明确新层不得替代旧层。

### 已修复 P1：放宽知识问答不能连带删除方案确定性校验

- `where`：修订后的 `decision.md`、`gap.md`。
- `what`：若把现有约 374 行发布门整体替换成实验薄门，方案对话也会失去 verdict、批准方案和患者事实一致性校验。
- `why`：知识科普与规则评估后的方案解释不是同一风险层；全局替换会破坏旧功能。
- `fix`：已明确只在知识问答拆薄门，方案对话保留原确定性校验。

复核后未发现其他 P0–P2。没有引入新模型、API、运行依赖或训练任务。

## 医学安全视角

未发现 P0–P2。薄门不判断一般医学知识正确性，只阻止明确诊断、个体药物动作、具体操作参数、特殊人群动作和疗效保证；证型与方案仍由原规则/truth 裁决。

## 汇总

| level | 数量 | 状态 |
|---|---:|---|
| P0 | 0 | 清零 |
| P1 | 2 | 已修复并复核 |
| P2 | 0 | 清零 |
| P3 | 0 | 无需扩张 |

结论：取消小模型符合当前成本；不建议完全取消所有兜底。调研取舍可交给作者判断；这不是运行时实现、临床批准或生产就绪证明。若获授权，应隔离接入实验 017 的无模型薄门并补完整回归。

# 管理后台最小分形样本复诊

> 日期：2026-08-27
> 基线：`origin/main@7e76363`
> 候选：`origin/main@5967e54`（#364–#368 均已合并）

## 结论

**停止继续拆分。** 本轮已经把管理后台从“入口文件吸收全部功能”改为按真实变化轴定位，
同时保住测试密度和外部行为。继续提取 Login、Overview，或把消息与文件各建一层目录，
当前只会增加跳转与样板，不会进一步缩小典型变更的理解范围。

## 量化账单

| 指标 | 拆分前 | 拆分后 | 解释 |
| --- | ---: | ---: | --- |
| `AdminApp.tsx` | 1348 行 | 212 行 | 只保留会话、刷新、导航、工作台和组合 |
| 管理后台 `src` 文件 | 7 | 13 | 新增 6 个有名边界，是明确导航代价 |
| 最大新功能文件 | 无 | 453 行 | 内容管理保持一个完整变化轴，未按行数硬切 |
| 用例密度 | 6.3/千行 | 6.3/千行 | 信息量代理未下降 |
| TSX 冗余率 | 13.2% | 12.8% | 没有用复制模板换整齐 |
| 导航成本 | 14.23 bit | 14.26 bit | +0.03，来自新增边界，需由典型改动范围下降来偿还 |
| 行数中位数 | 121 | 120 | 整体没有长出一批中等文件 |
| 超 600 行占比 | 14.4% | 13.8% | `AdminApp` 已退出存量长文件护栏 |

## 典型改动现在落在哪里

| 变化 | 主要文件 | 通常需理解的其他边界 |
| --- | --- | --- |
| 文章/视频草稿、上传、预览、发布 | `features/content-manager.tsx` | 展示变化时才看 `content-views.tsx` |
| 正文编辑、素材绑定、患者预览 | `features/content-views.tsx` | 稳定 DTO 与共享 UI |
| 知识文件夹、索引、测试、启用 | `features/knowledge-manager.tsx` | 稳定 DTO、共享 UI、客户端 |
| 消息与文件辅助运营 | `features/support-managers.tsx` | 稳定 DTO、共享 UI、客户端 |
| 会话、刷新、导航、工作台 | `AdminApp.tsx` | 各功能只作为组合组件 |

依赖方向为 `AdminApp → features → admin-ui/admin-contracts/client`；内容管理单向使用内容叶子，
未发现 feature 反向依赖 `AdminApp`、循环依赖、重复契约或只转发一层的空壳文件。

## 验证证据

- #364–#368 每条均独立提交、PR、CI、合并并自动关闭 Issue。
- 每轮 `npm run test:acceptance`、完整 `npm run check`、真实 Chromium、manifest、结构与 diff 检查通过。
- 完整本地门禁为 436 项：358 通过、78 项因未配置 PostgreSQL/S3 按约定跳过、0 失败；远端 CI 补齐 PostgreSQL、MinIO/S3 与 OCI 镜像。
- 演化护栏正常树通过；临时增加第 19 个顶层目录后明确 exit 1，探针已清除。

## 三视角评审

- 患者视角：无患者可见文案、流程或展示行为变化；真实浏览器文章发布链通过。
- 工程视角：入口职责收敛，依赖单向，每个功能可独立回退；新增文件数与导航成本轻微上升已如实记录。
- 医学安全视角：知识状态机、单资料测试、明确启用、失败关闭与视频安全提示均未改变。

未发现 P0、P1、P2。P3 观察项：后续两个真实后台变更应记录主要触达文件；若仍频繁跨三个以上
功能边界，再依据真实传播路径调整，而不是继续按行数拆分。

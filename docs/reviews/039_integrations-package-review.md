# 外部适配器包交付评审

- 基线：`codex/src-fractal-05`，源自 `origin/main@069bb1a`
- 对象：`src/packages/kangmin-integrations/`、组合根/测试导入、构建与架构护栏
- 方法：sequential-thinking 六步元反思；`km-review` 患者可懂性、工程事实、医学安全三视角

## 患者可懂性视角

- P0–P2：未发现。
- 本轮没有修改患者文案、页面、交互或错误消息；患者、小程序和管理后台路径均由完整门禁与真实浏览器覆盖。

## 工程事实视角

- P0–P2：未发现。
- 12 个适配器仅移动路径，外层统一使用 `@kangmin/integrations/*`；包门禁阻止反向依赖 app/http/cli/database/evals。
- 根 manifest、构建顺序和 Docker builder/runner 都显式携带 integrations，覆盖此前出现过的 workspace 软链接目标缺失变式。
- 旧 `infrastructure/` 已清空并从迁移白名单移除。数据库护栏对旧目录不存在的变式已修复并复测。
- 限制：本机未配置真实 S3，8 条存储契约如实跳过；真实 S3 与 OCI 由 PR CI 放行。`npm audit` 仍报告一个既有 high，未在结构迁移中混入依赖升级。

## 医学安全视角

- P0–P2：未发现。
- 模型提示词、候选校验、临床注册表、问卷、证型、分期、方案和知识发布门禁均未修改；未配置、超时、非法端点和上游失败的既有失败关闭行为由窄测及完整回归覆盖。
- 密钥仍只来自服务端配置，结构化日志字段没有扩张，未把请求正文、令牌或健康信息带入日志。

## 结论

当前 P0、P1、P2 为 0，可以进入 PR/CI。实现满足 #348 的外部适配器单包边界，但仍须以 CI 的真实 S3、完整 PostgreSQL 回归和 OCI 镜像绿灯作为合并条件；整体复杂度结论留到 #350 最终复诊。

# 原生微信小程序非智能体薄壳复核

> 2026-08-17。评审基线：实现提交 `bb60564`（基于 `main@e574ab8`）；范围为
> `src/miniprogram/`、对应检查与测试，以及本轮事实文档。

## 结论

P0、P1、P2 为 0。实现可以进入 PR 合并流程；自动化和游客模式编译不代表真实
AppID、真机、客户、医学或上线验收。

## 患者视角

未发现阻塞问题。症状量表明确只用于个人变化回看，不构成诊断或严重程度判定；内容空态、
登录未配置和问助手待接入均显式呈现，没有把内部载荷、假数据或未实现能力展示成可用结果。

## 工程视角

未发现遗留 P0–P2。公共内容请求不触发登录；私有请求只有在配置允许时才执行
`wx.login → /v1/auth/wechat → Bearer`，401 清理旧令牌并仅重试一次。登录关闭回归证明不会
调用微信身份或服务端身份接口。页面清单、事件绑定、JS 语法与 AppID/密钥边界已纳入
`npm run check`。视觉返工抽查曾发现五格底栏仍按四入口编号，导致“我的”页高亮“日历”；
现已把日历/我的索引修正为 3/4，并增加机械门禁防止回归。品牌横幅和助手图直接复用 Web
正本，检查逐字节校验两端一致。

## 医学安全视角

未发现问题。本轮没有在小程序复制决策树，也没有修改问卷、证型、期别、方案、知识检索、
模型或任何医学 truth；问助手只是不可交互的边界说明。症状记录和趋势只展示服务端患者数据，
不从分数自行生成诊断或方案。

## 非阻塞边界

- `project.config.json` 的 AppID 为空，开发者工具游客模式存在微信内部接口告警；问题面板为
  0，首页、日历、我的和科普空态完成模拟器目测，但仍未取得真实 AppID 和真机证据。
- 当前试用地址不是客户正式小程序业务域名，微信登录保持关闭；这些属于客户账号和正式资源
  齐备后的联调项，不能在本轮用代码推断完成。
- 问助手和原生消息明确不在本轮实现范围，仍需分别按客户路线和报价差距安排后续工作。

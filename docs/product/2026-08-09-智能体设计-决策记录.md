# 智能体设计决策记录（分角色 · 可回看）

- 创建：2026-08-09
- 目的：AI 决策、作者决策、客户决策分角色记录，分清楚意图；后续回看、反思、修改时能定位"谁在什么依据下定的"。
- 决策类型：
  - **客户决策**：客户已确认/待客户确认的医学与材料层面决定（权威依据为《评估问卷与规则确认》decision.md）
  - **作者决策**：作者拍板的产品与交付层面决定（2026-08-09 AskUserQuestion 记录）
  - **AI 决策**：Agent 为实现层做出的技术判断（两轮评审+codex 评审闭环后的实现选择），作者未逐条拍板，可回看修正
- 状态：`定稿`（执行依据）| `供医学审核`（医学层面待客户）| `待客户确认`（客户反馈后回写）

## 客户决策（见 docs/product/2026-08-09-评估问卷与规则确认-decision.md，8 项 A）

Q1-Q5 全部选 A：寒热错杂判定（Q11B+寒象）、人群题、确诊题、Q6 信息收集、期别以《前置规则》为准；补充 ⑥ 缓解期字面执行（Q1C 即缓解期）、⑦ 证型简称、⑧ 欢迎语页面版。**作者代选，客户未回复，标"待客户确认"**。

## 作者决策（2026-08-09 拍板 8 项）

| # | 决策 | 状态 |
|---|---|---|
| 1 | 问卷**多选题保真**：对话渲染客户问卷原题原选项按钮，传选项值（q1=B），服务端确定性映射；Q12-14 保留提问仅收集 | 定稿 |
| 2 | 采纳**证型 v3 七规则**：寒热错杂（怕热+寒象）字面优先客户树；肺气虚寒/肾阳不足按客户树默认分支 | 供医学审核 |
| 3 | **删 APP-01 症状计数转介**：确诊题承担适用范围门禁；缓解期无症状者正常拿调体方案 | 定稿 |
| 4 | **儿童可用**：删 SAF-07，儿童走小儿方案（客户资料含小儿手法） | 供医学审核 |
| 5 | 部署前**清空线上旧会话**（预览库） | 定稿 |
| 6 | **有回退线**：16:00 未全绿按优先级裁减；最坏保留现状 cc79ac5 向客户说明 | 定稿 |
| 7 | 今天**冻结**：candidate→approved + clinical-rules-v1 | 定稿 |
| 8 | **配模型 key**：部署时确认/配置线上 DEEPSEEK key，演示走自由文本+AI 提取（按钮兜底） | 定稿 |

## AI 决策（实现层技术判断，可回看修正）

| # | 决策 | 依据 | 回看指引 |
|---|---|---|---|
| A1 | T1/T2 拆双规则（T1a/b、T2a/b）实现"口渴∧¬(怕热∧寒象)"排除语义 | 扁平 anyOf 无法表达嵌套排除，拆 OR 规则按 code 去重 | 若 DSL 扩展嵌套支持可还原单规则；改动点 rule-package.ts |
| A2 | 32 组合中 1 个罕见组合（怕热单独）→ no_match 兜底，渲染门诊提示后**结束会话** | 评审穷举：该组合客户树无判定；no_match 必发生在字段闭环后（未答全会 pending 补问），无新信息可收集，结束会话并渲染 message（评审并发 P1-1 修正：原记录"不结束会话"与实现不符，已按实现定稿） | 客户反馈后定归属 |
| A3 | cold_intolerance（形寒肢冷）退出判定仅信息收集 | 客户树第 5/6 步未用 Q11；T4 改为四肢凉+无热象 | 若客户确认肾阳不足需形寒肢冷再改 T4 |
| A4 | 确诊"不确定"→no 仅 diagnosed_confirmed 字段白名单特判 | unknown 不进条件评估；通用映射会击穿 SAF-01 | 实现于内核 screening 评估处 |
| A5 | 选项值映射表（q1=B→paroxysmal_sneezing=yes 等）由服务端确定性维护 | codex 建议多选题保真；前端不复制映射 | 新增映射表文件，测试覆盖 |
| A6 | findApprovedPlanBundle 双方案查询（ACUTE+证型 × audience） | codex P0-1：原查询忽略期别/人群 | 端口与 SQLite/PG 实现 |
| A7 | seed 禁"待医学补充"占位，risks/contraindications 用已确认固定文案 | 启用校验只查非空会放行占位 | seed 脚本 + 校验拒绝占位词 |
| A8 | 迁移仅 SQLite 为主，PG 按部署预检确认后决定 | 线上大概率 SQLite；KANGMIN_DATABASE_URL 存在即 PG | 部署预检第 1 项 |
| A9 | 决策凭证：plan_id 落调体方案（约定）；plan_bundle 不入库；消息 contentHash 唯一权威 | 评审 P2-5/架构 P1-5 | 写入 sqlite-conversation-repository |
| A10 | 清空旧会话按子表到父表事务清理 | codex P1-1：父表删除被无级联外键阻挡 | 部署脚本实现 |

## 待客户确认清单（供医学审核，客户反馈后回写）

1. 寒热错杂判定条件（T5 字面优先客户树）
2. 儿童进入完整评估获外治方案（删 SAF-07）
3. 证型规则整体（v3 七规则与客户 6 步树的偏离，见偏离清单）
4. 主症计数门槛删除（确诊题替代）
5. 多选题保真交互（与"对话式"字面的关系）

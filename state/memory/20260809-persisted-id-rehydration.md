# 持久化 ID 必须权威回读

## 适用场景

前端把会话、草稿或流程实体的 ID 存入浏览器存储，但正文、步骤和终态只保留在组件内存中。

## 规则

- 刷新恢复不能只取 ID 后直接续写，必须先从应用服务回读实体正文、顺序、版本和终态。
- 服务端已结束、过期或不存在时，界面应进入明确的只读或失效状态，并提供显式新建入口；不得沿用旧 ID 重试，也不得静默把同一输入重发到新实体。
- 历史正文由服务端按所属主体隔离读取；加密字段解密后还要校验内容哈希，失败不得伪装成空历史。
- 验收至少覆盖刷新恢复、新建 ID 唯一、历史切换、终态禁写、跨主体不可见。

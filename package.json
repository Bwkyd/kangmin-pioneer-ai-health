{
  "name": "kangmin-pioneer-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "bin": {
    "kangmin": "./src/cli.mjs"
  },
  "scripts": {
    "build": "node --check src/cli.mjs && node src/cli.mjs --help > /dev/null",
    "test": "node --test src/tests/*.test.mjs",
    "lint": "node --check src/cli.mjs && node --check src/core/capabilities.mjs"
  }
}
